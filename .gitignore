# Grading results — see README, never push (Memo 076, Chapter 14)
grading-data/

# Node.js
node_modules/
npm-debug.log*
.npm/

# OS / Editor
.DS_Store
.idea/
.vscode/
*.swp

# Memo working dirs — never push (Memo 081)
.memo/

# Local environment overrides (no .env should live in this repo — see AGENTS.md)
.env
.env.local
