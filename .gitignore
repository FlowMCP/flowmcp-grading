# Grading results — see README, never push
grading-data/

# Grading island (Memo 097 PA-6 in-tree default <root>/_grading) + exports.
# Holds schema duplicates / proofs — never version.
_grading/
_exports/

# Node.js
node_modules/
npm-debug.log*
.npm/

# Test coverage + transient run artifacts
coverage/
.tmp/

# Local scratch scripts (underscore prefix) — never committed (Scripts-Policy)
scripts/_*.mjs

# OS / Editor
.DS_Store
.idea/
.vscode/
*.swp

# Local working notes — never push
.memo/

# Local environment overrides (no .env should live in this repo — see AGENTS.md)
.env
.env.local

# local recovery copies (legacy never deleted; git history is source of truth)
.trash/
.poc-087/
